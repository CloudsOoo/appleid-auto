# Apple ID 自动解锁系统 - 后续开发指南

## 📊 当前进度

**总体进度**: ████████████░░░░ 78% → 目标 100%

### ✅ 已完成
1. **架构设计** - 100%
2. **数据库设计** - 100% (15张表)
3. **数据库模型** - 100% (15个SQLAlchemy模型)
4. **Pydantic Schemas** - 100% (9个schema文件，50+ schemas)
5. **Service 层** - 30%
   - ✅ AuthService (完整)
   - ✅ CardService (完整)
   - ⏳ 其他 6个 Service
6. **HTML 安全过滤** - 100%
7. **配置管理** - 100%
8. **数据库初始化脚本** - 100%
9. **Docker 部署配置** - 100%
10. **技术文档** - 85% (7个完整文档)

---

## 🔄 继续开发计划

### 阶段一：完成 Service 层 (剩余 6个)

#### 1. AppleAccountService - Apple ID 管理服务
**文件**: `backend/app/services/apple_account_service.py`

**必须实现的方法**:
```python
class AppleAccountService:
    @staticmethod
    async def create_account(...) -> AppleAccount:
        """创建 Apple ID 账号"""

    @staticmethod
    async def update_account(...) -> AppleAccount:
        """更新账号信息"""

    @staticmethod
    async def delete_account(...) -> bool:
        """删除账号"""

    @staticmethod
    async def get_account(...) -> AppleAccount:
        """获取账号详情"""

    @staticmethod
    async def get_account_list(...) -> tuple[List[AppleAccount], int]:
        """获取账号列表（支持分页、搜索、过滤）"""

    @staticmethod
    async def import_accounts(...) -> dict:
        """批量导入账号"""

    @staticmethod
    async def export_accounts(...) -> List[dict]:
        """批量导出账号"""

    @staticmethod
    async def get_password_history(...) -> List[PasswordHistory]:
        """获取密码历史"""

    @staticmethod
    async def update_password(...) -> bool:
        """更新密码并记录历史"""
```

**核心逻辑**:
- 权限检查（账号数量限制）
- 密码加密存储
- 密保问题加密
- 标签管理
- 批量导入时的数据验证和错误处理
- 导出时过滤敏感信息（根据权限）

---

#### 2. SharePageService - 分享页服务
**文件**: `backend/app/services/share_page_service.py`

**必须实现的方法**:
```python
class SharePageService:
    @staticmethod
    async def create_share_page(...) -> SharePage:
        """创建分享页"""

    @staticmethod
    async def update_share_page(...) -> SharePage:
        """更新分享页"""

    @staticmethod
    async def delete_share_page(...) -> bool:
        """删除分享页"""

    @staticmethod
    async def get_share_page_list(...) -> tuple[List[SharePage], int]:
        """获取分享页列表"""

    @staticmethod
    async def access_share_page(...) -> dict:
        """访问分享页（公开接口）"""

    @staticmethod
    async def validate_custom_html(...) -> tuple[bool, str]:
        """验证自定义 HTML"""

    @staticmethod
    async def filter_custom_html(...) -> dict:
        """过滤自定义 HTML（使用 html_filter）"""

    @staticmethod
    async def record_access_log(...) -> bool:
        """记录访问日志"""

    @staticmethod
    async def get_access_logs(...) -> List[SharePageLog]:
        """获取访问日志"""

    @staticmethod
    async def get_stats(...) -> dict:
        """获取统计信息"""
```

**核心逻辑**:
- **HTML 安全过滤** - 调用 `app.utils.html_filter`
- 权限检查（分享页数量限制、自定义 HTML 权限）
- slug 唯一性检查
- 密码验证
- 访问次数控制
- 有效期检查
- 域名白名单验证
- 随机/顺序账号选择
- 访问日志记录
- IP 地理位置解析（可选）

---

#### 3. TaskService - 任务服务
**文件**: `backend/app/services/task_service.py`

**必须实现的方法**:
```python
class TaskService:
    @staticmethod
    async def create_unlock_task(...) -> List[UnlockTask]:
        """创建解锁任务"""

    @staticmethod
    async def get_task_list(...) -> tuple[List[UnlockTask], int]:
        """获取任务列表"""

    @staticmethod
    async def get_task(...) -> UnlockTask:
        """获取任务详情"""

    @staticmethod
    async def cancel_task(...) -> bool:
        """取消任务"""

    @staticmethod
    async def get_task_stats(...) -> dict:
        """获取任务统计"""

    @staticmethod
    async def select_best_node(...) -> Optional[Node]:
        """选择最佳节点（负载均衡）"""

    @staticmethod
    async def select_best_proxy(...) -> Optional[Proxy]:
        """选择最佳代理"""
```

**核心逻辑**:
- 权限检查（解锁次数限制、解锁间隔）
- 节点选择算法（负载均衡）
- 代理选择算法（成功率优先）
- 任务优先级处理
- 与 Celery 集成（将任务加入队列）

---

#### 4. ProxyService - 代理服务
**文件**: `backend/app/services/proxy_service.py`

```python
class ProxyService:
    @staticmethod
    async def add_proxy(...) -> Proxy:
        """添加代理"""

    @staticmethod
    async def test_proxy(...) -> dict:
        """测试代理可用性"""

    @staticmethod
    async def get_proxy_list(...) -> tuple[List[Proxy], int]:
        """获取代理列表"""

    @staticmethod
    async def get_available_proxy(...) -> Optional[Proxy]:
        """获取可用代理（轮询算法）"""

    @staticmethod
    async def update_proxy_stats(...) -> bool:
        """更新代理统计"""
```

---

#### 5. NodeService - 节点服务
**文件**: `backend/app/services/node_service.py`

```python
class NodeService:
    @staticmethod
    async def register_node(...) -> Node:
        """注册节点"""

    @staticmethod
    async def heartbeat(...) -> bool:
        """处理心跳"""

    @staticmethod
    async def get_online_nodes(...) -> List[Node]:
        """获取在线节点"""

    @staticmethod
    async def check_node_health(...) -> bool:
        """检查节点健康"""
```

---

#### 6. PermissionService - 权限服务
**文件**: `backend/app/services/permission_service.py`

```python
class PermissionService:
    @staticmethod
    async def check_permission(...) -> bool:
        """检查权限"""

    @staticmethod
    async def check_quota(...) -> tuple[bool, str]:
        """检查配额"""

    @staticmethod
    async def update_permission(...) -> bool:
        """更新权限"""

    @staticmethod
    async def reset_daily_counters(...) -> bool:
        """重置每日计数器"""
```

---

### 阶段二：实现依赖注入和中间件

**文件**: `backend/app/api/dependencies.py`

```python
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials

security = HTTPBearer()

async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(security),
    db: AsyncSession = Depends(get_db)
) -> User:
    """获取当前用户"""

async def get_current_active_user(
    current_user: User = Depends(get_current_user)
) -> User:
    """获取当前激活用户"""

async def get_current_admin(
    current_user: User = Depends(get_current_user)
) -> User:
    """获取当前管理员"""

async def check_permission(permission_name: str):
    """权限检查装饰器"""

async def rate_limit(max_requests: int = 60):
    """限流装饰器"""
```

---

### 阶段三：实现所有 API 端点

按模块顺序实现（每个模块一个文件）:

1. `backend/app/api/v1/endpoints/auth.py` - 认证 API
2. `backend/app/api/v1/endpoints/users.py` - 用户 API
3. `backend/app/api/v1/endpoints/cards.py` - 卡密 API
4. `backend/app/api/v1/endpoints/accounts.py` - Apple ID API
5. `backend/app/api/v1/endpoints/tasks.py` - 任务 API
6. `backend/app/api/v1/endpoints/share_pages.py` - 分享页 API
7. `backend/app/api/v1/endpoints/proxies.py` - 代理 API
8. `backend/app/api/v1/endpoints/nodes.py` - 节点 API
9. `backend/app/api/v1/endpoints/admin.py` - 管理员 API

每个 API 端点必须包含:
- 请求验证（Pydantic）
- 权限检查（Depends）
- 错误处理
- 返回标准格式

---

### 阶段四：实现 Celery 任务系统

**文件结构**:
```
backend/app/tasks/
├── __init__.py
├── celery_app.py          # Celery 应用配置
├── check_tasks.py         # 检测任务
├── unlock_tasks.py        # 解锁任务
├── maintenance_tasks.py   # 维护任务
└── utils.py               # 任务工具函数
```

**核心任务**:
1. `check_accounts_task` - 定时检测所有账号
2. `unlock_account_task` - 执行单个账号解锁
3. `disable_2fa_task` - 关闭 2FA
4. `change_password_task` - 修改密码
5. `remove_devices_task` - 删除设备
6. `check_proxy_health` - 代理健康检查
7. `check_node_health` - 节点健康检查
8. `reset_daily_counters` - 重置每日计数

**Celery Beat 配置**:
```python
# celerybeat-schedule.py
beat_schedule = {
    'check-accounts-every-hour': {
        'task': 'app.tasks.check_tasks.check_accounts_task',
        'schedule': crontab(minute=0),  # 每小时
    },
    'check-proxy-health': {
        'task': 'app.tasks.maintenance_tasks.check_proxy_health',
        'schedule': 300.0,  # 每5分钟
    },
    # ...
}
```

---

### 阶段五：前端开发

#### 基础配置

**文件**: `frontend/vite.config.js`
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src')
    }
  },
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  }
})
```

**文件**: `frontend/src/utils/request.js` - Axios 封装
**文件**: `frontend/src/router/index.js` - 路由配置
**文件**: `frontend/src/stores/user.js` - 用户状态管理

#### 核心页面（按优先级）

1. **登录/注册页面**
   - `frontend/src/pages/Login.vue`
   - `frontend/src/pages/Register.vue`

2. **用户面板**
   - `frontend/src/pages/Dashboard.vue`
   - 显示统计信息
   - 快捷操作

3. **Apple ID 管理**
   - `frontend/src/pages/Accounts/List.vue`
   - `frontend/src/pages/Accounts/Create.vue`
   - `frontend/src/pages/Accounts/Import.vue`

4. **分享页管理**
   - `frontend/src/pages/SharePages/List.vue`
   - `frontend/src/pages/SharePages/Create.vue`
   - 集成富文本编辑器（自定义 HTML）

5. **分享页展示**
   - `frontend/src/pages/Public/SharePage.vue`

6. **管理后台**
   - `frontend/src/pages/Admin/Users.vue`
   - `frontend/src/pages/Admin/Cards.vue`
   - `frontend/src/pages/Admin/Generate Cards.vue`

---

### 阶段六：测试

**文件结构**:
```
backend/tests/
├── test_services/
│   ├── test_auth_service.py
│   ├── test_card_service.py
│   └── ...
├── test_api/
│   ├── test_auth.py
│   ├── test_cards.py
│   └── ...
└── conftest.py  # Pytest 配置
```

**测试覆盖**:
- Service 层单元测试
- API 集成测试
- HTML 过滤测试
- 权限测试
- Celery 任务测试

---

### 阶段七：文档补充

需要新增的文档:
1. `docs/08-权限系统说明.md`
2. `docs/09-卡密系统使用手册.md`
3. `docs/10-节点部署指南.md`
4. `docs/11-用户使用手册.md`
5. `docs/12-管理员使用手册.md`
6. `docs/13-常见问题FAQ.md`

---

## 🚀 快速继续开发

### 方法1：按阶段顺序开发

```bash
# 1. 继续实现 Service 层
# 创建 AppleAccountService
# 创建 SharePageService
# ... 其他 Service

# 2. 实现依赖注入
# 创建 dependencies.py

# 3. 实现 API 端点
# 从 auth.py 开始，逐个实现

# 4. 实现 Celery 任务
# 配置 Celery，实现核心任务

# 5. 前端开发
# 配置 Vite，实现页面

# 6. 测试
# 编写测试用例

# 7. 文档
# 补充文档
```

### 方法2：按功能模块开发

每个模块包含 Service + API + 前端 + 测试：

```
模块1：用户认证
├── Service: ✅ AuthService
├── API: ⏳ auth.py
├── 前端: ⏳ Login.vue, Register.vue
└── 测试: ⏳ test_auth.py

模块2：卡密系统
├── Service: ✅ CardService
├── API: ⏳ cards.py
├── 前端: ⏳ CardActivation.vue, Admin/Cards.vue
└── 测试: ⏳ test_card.py

... 依此类推
```

---

## 📝 代码模板

### Service 模板
```python
"""
XXX 服务
"""
from typing import Optional, List
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, and_, func

class XXXService:
    @staticmethod
    async def create(...) -> Model:
        """创建"""
        # 1. 权限检查
        # 2. 数据验证
        # 3. 创建记录
        # 4. 记录日志
        pass

    @staticmethod
    async def get_list(...) -> tuple[List[Model], int]:
        """获取列表"""
        # 1. 构建查询
        # 2. 应用过滤
        # 3. 分页
        # 4. 返回结果和总数
        pass
```

### API 端点模板
```python
from fastapi import APIRouter, Depends, HTTPException
from app.api.dependencies import get_current_user, get_db

router = APIRouter()

@router.post("/xxx")
async def create_xxx(
    data: XXXCreate,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """创建 XXX"""
    try:
        result = await XXXService.create(db, current_user.id, ...)
        return {"code": 200, "data": result}
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))
```

---

## ⏱️ 预计工作量

| 阶段 | 预计代码量 | 预计时间 |
|------|-----------|---------|
| Service 层（剩余6个） | ~1200行 | 继续 |
| 依赖注入/中间件 | ~500行 | 继续 |
| API 端点（50+个） | ~2500行 | 继续 |
| Celery 任务 | ~1500行 | 继续 |
| 前端开发 | ~5000行 | 继续 |
| 测试 | ~2000行 | 继续 |
| 文档 | 6个文档 | 继续 |

**总计**: 约 13,000 行代码 + 6个文档

---

## 💡 建议

1. **优先级**: Service → API → Celery → 前端 → 测试 → 文档
2. **增量开发**: 每完成一个模块就提交代码
3. **测试驱动**: 边开发边测试
4. **文档同步**: 关键功能立即编写文档

---

**更新时间**: 2025-11-20
**当前状态**: Service 层 30% 完成，准备继续开发
**下一步**: 实现 AppleAccountService
